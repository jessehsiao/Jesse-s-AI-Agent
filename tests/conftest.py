"""Shared pytest fixtures for the strands-bedrock-agent test suite.

Placeholder created in task 1.3. Fixtures (mock ``MCPClient``, mock
``BedrockModel``, captured-log handler, ``Config`` factories, etc.) are
added by later tasks as each module is implemented.
"""
